# ┌───────────────────────────────────────────────────────────────────────────────
# │ APP
# └───────────────────────────────────────────────────────────────────────────────
# Dirección y puerto donde corre la API
HOST=0.0.0.0
PORT=8000

# ┌───────────────────────────────────────────────────────────────────────────────
# │ POSTGRES (desarrollo)
# └───────────────────────────────────────────────────────────────────────────────
# Usuario y contraseña para dev/local
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=globant_challenge

# String de conexión para desarrollo (usa POSTGRES_PASSWORD)
DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# ┌───────────────────────────────────────────────────────────────────────────────
# │ POSTGRES (producción con Docker Secrets)
# └───────────────────────────────────────────────────────────────────────────────
# Descomenta estas líneas en entornos que monten el secret db_password en /run/secrets:
# POSTGRES_PASSWORD_FILE=/run/secrets/db_password
# DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:$(cat ${POSTGRES_PASSWORD_FILE})@db:5432/${POSTGRES_DB}
